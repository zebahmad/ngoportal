<%= form_for(child) do |f| %>
  <% if child.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(child.errors.count, "error") %> prohibited this child from being saved:</h2>

      <ul>
      <% child.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :photograph %>
    <%= f.text_field :photograph %>
  </div>

  <div class="field">
    <%= f.label :enrolled_on %>
    <%= f.datetime_select :enrolled_on %>
  </div>

  <div class="field">
    <%= f.label :created_on %>
    <%= f.datetime_select :created_on %>
  </div>

  <div class="field">
    <%= f.label :last_updated_on %>
    <%= f.datetime_select :last_updated_on %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
