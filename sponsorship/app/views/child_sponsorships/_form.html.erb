<%= form_for(child_sponsorship) do |f| %>
  <% if child_sponsorship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(child_sponsorship.errors.count, "error") %> prohibited this child_sponsorship from being saved:</h2>

      <ul>
      <% child_sponsorship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :child_id %>
    <%= f.text_field :child_id %>
  </div>

  <div class="field">
    <%= f.label :sponsor_id %>
    <%= f.text_field :sponsor_id %>
  </div>

  <div class="field">
    <%= f.label :sponsorship_type %>
    <%= f.text_field :sponsorship_type %>
  </div>

  <div class="field">
    <%= f.label :start_from %>
    <%= f.datetime_select :start_from %>
  </div>

  <div class="field">
    <%= f.label :valid_till %>
    <%= f.datetime_select :valid_till %>
  </div>

  <div class="field">
    <%= f.label :periodicity %>
    <%= f.text_field :periodicity %>
  </div>

  <div class="field">
    <%= f.label :event %>
    <%= f.text_field :event %>
  </div>

  <div class="field">
    <%= f.label :comments %>
    <%= f.text_area :comments %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
